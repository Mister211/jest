import { division } from "./division";
Run|Debug
test("Test", () => {
  expect(division(10, 2)).toBe(5);
});
Run|Debug
test("Test", () =>{
    expect(division(1, 0).tobe("El divisor debe ser diferente a cero papa bello"));
});
